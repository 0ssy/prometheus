[package]
name = "tensor-engine"
version = "0.1.0"
description = "P6 High Performance Engine — tensor/memory/vector engines (Rust)"
edition = "2021"
rust-version = "1.77"
license = "MIT"

[dependencies]

[features]
default = []

[dev-dependencies]
