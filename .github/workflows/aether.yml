name: Aether AI Runtime

# Builds and tests the standalone `ai-runtime` Rust crate only. This must NOT
# invoke `cargo tauri build` (needs WebView2/NSIS/webkit2gtk) so the verified
# RC1 desktop build stays isolated. Mirrors the cross-platform matrix used by
# the Python `windows.yml` / `linux.yml` jobs.

on:
  push:
    branches: ["main", "feature/aether"]
    paths:
      - "crates/ai-runtime/**"
      - "src-tauri/Cargo.toml"
      - "src-tauri/src/lib.rs"
      - ".github/workflows/aether.yml"
  pull_request:
    paths:
      - "crates/ai-runtime/**"
      - "src-tauri/Cargo.toml"
      - "src-tauri/src/lib.rs"
      - ".github/workflows/aether.yml"
  workflow_dispatch:

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  aether:
    name: ai-runtime (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            crates/ai-runtime/target
          key: ${{ runner.os }}-aether-${{ hashFiles('crates/ai-runtime/Cargo.toml') }}
          restore-keys: |
            ${{ runner.os }}-aether-

      - name: Build ai-runtime
        run: cargo build --manifest-path crates/ai-runtime/Cargo.toml --verbose

      - name: Test ai-runtime
        run: cargo test --manifest-path crates/ai-runtime/Cargo.toml --verbose

      - name: Check formatting
        run: cargo fmt --manifest-path crates/ai-runtime/Cargo.toml -- --check

      - name: Clippy
        run: cargo clippy --manifest-path crates/ai-runtime/Cargo.toml -- -D warnings
